; =============================================================================
; GENERATED BY TERRAFORM — DO NOT EDIT
; =============================================================================
; Source of truth: terraform/main.tf (resource "local_file" "ansible_inventory")
; Regenerate with: make apply   (or: terraform -chdir=terraform apply)
;
; This file is gitignored: it names a private key path and changes on every
; rebuild.
; =============================================================================

[${group}]
${hostname} ansible_host=${ip_address}

[${group}:vars]
ansible_user=${ssh_user}
ansible_ssh_private_key_file=${ssh_private_key_path}
ansible_python_interpreter=/usr/bin/python3

; Passed through from Terraform so a single variable drives both layers.
wazuh_version=${wazuh_version}
wazuh_manager_address=${ip_address}
host_timezone=${timezone}
